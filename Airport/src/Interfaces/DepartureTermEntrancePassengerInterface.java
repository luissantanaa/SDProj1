package Interfaces;

import airport.Passenger;

/**
 * 
 * Interface de acesso do passageiro ao Monitor Departure Terminal Entrance
 *
 */

public interface DepartureTermEntrancePassengerInterface {
	public boolean prepareNextLeg(Passenger P);
}
